#include <stdio.h>
#include<conio.h>
int main(void) 
{
int i,a;
clrscr();
printf("\n enter the value of a");
scanf("\n%d",&a);
for(i=0;i<=a;i++)
{
	printf("\n the number of i is==> %d",i);
}
getch();
}
